
// Destructuring number dari props, ini sama aja dengan props.number
const NumberDisplay = ({ number }) => {
  return (
    <div>
      <h2>{number}</h2>
    </div>
  )
}

export default NumberDisplay
